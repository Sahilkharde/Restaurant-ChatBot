# Environment files — NEVER commit these
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Logs
*.log
log/

# Build output — block backend dist, but allow pre-built frontend dist
backend/dist/
backend/node_modules/
.angular/
# frontend/dist is intentionally committed so Render can serve without building

# Mercurial
.hg/
.hgignore

# OS files
.DS_Store
Thumbs.db

# FAISS index (large binary, generated locally)
content/faiss.index
